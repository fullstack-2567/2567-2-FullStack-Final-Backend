<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8">
  <title>รายงาน</title>
  <link href="https://fonts.googleapis.com/css2?family=Sarabun&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Sarabun', sans-serif;
      padding: 40px;
    }
    h1 {
      color: #3498db;
    }
    table {
      width: 100%;
      border-collapse: collapse;
      margin-bottom: 40px;
    }
    th, td {
      border: 1px solid #ccc;
      padding: 8px 12px;
      text-align: left;
    }
    th {
      background-color: #f2f2f2;
    }
  </style>
</head>
<body>

  <h1>รายงานสรุป<%= reportType === 'project' ? 'การอนุมัติโครงการ	' : 'การเรียนออนไลน์' %></h1>
  <p>สร้างโดย: <%= userName %></p>
  <p>วันที่: <%= date %></p>

  <% if (reportType === 'elearning') { %>
    <% if (!isUserReport) { %>
      <h2>สรุปวิชาเรียน</h2>
      <table>
        <thead>
          <tr>
            <th>ลำดับที่</th>
            <th>ชื่อวิชาเรียน</th>
            <th>ประเภทวิชาเรียน</th>
            <th>จำนวนผู้เข้าเรียน</th>
            <th>จำนวนผู้เรียนจบ</th>
          </tr>
        </thead>
        <tbody>
          <% courseStats?.forEach((course, index) => { %>
            <tr>
              <td><%= index + 1 %></td>
              <td><%= course.contentName %></td>
              <td><%= course.contentCategory || '-' %></td>
              <td><%= course.learners %></td>
              <td><%= course.completed %></td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    <% } else { %>
      <h2>สรุปผู้เข้าเรียน</h2>
      <table>
        <thead>
          <tr>
            <th>ลำดับที่</th>
            <th>ชื่อผู้เรียน</th>
            <th>จำนวนวิชาเรียนที่เข้าเรียน</th>
            <th>จำนวนวิชาเรียนที่เรียนจบ</th>
          </tr>
        </thead>
        <tbody>
          <% userCourseStats?.forEach((user, index) => { %>
            <tr>
              <td><%= index + 1 %></td>
              <td><%= user.name %></td>
              <td><%= user.enrolled %></td>
              <td><%= user.completed %></td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    <% } %>
  <% } else if (reportType === 'project') { %>
    <% if (!isUserReport) { %>
      <h2>สรุปโครงการ</h2>
      <table>
        <thead>
          <tr>
            <th>ลำดับที่</th>
            <th>ประเภทโครงการ</th>
            <th>จำนวนโครงการที่ส่ง</th>
            <th>จำนวนโครงการที่ผ่านการอนุมัติ</th>
          </tr>
        </thead>
        <tbody>
          <% projectTypeStats?.forEach((entry, index) => { %>
            <tr>
              <td><%= index + 1 %></td>
              <td><%= entry.type %></td>
              <td><%= entry.submitted  %></td>
              <td><%= entry.passed %></td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    <% } else { %>
      <h2>สรุปผู้ส่งโครงการ</h2>
      <table>
        <thead>
          <tr>
            <th>ลำดับที่</th>
            <th>ชื่อผู้ส่งโครงการ</th>
            <th>จำนวนโครงการที่ส่ง</th>
            <th>จำนวนโครงการที่ผ่านการอนุมัติ</th>
            <th>จำนวนโครงการที่ไม่ผ่านการอนุมัติ</th>
          </tr>
        </thead>
        <tbody>
          <% projectUserStats?.forEach((entry, index) => { %>
            <tr>
              <td><%= index + 1 %></td>
              <td><%= entry.name %></td>
              <td><%= entry.submitted %></td>
              <td><%= entry.passed %></td>
              <td><%= entry.failed %></td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    <% } %>
  <% } %>    

</body>
</html>
